---
name: human-maintainability
version: 1.0.0
description: "对指定编程项目进行人类可维护性评估（HMAP，Human Maintainability Assessment of Programming Projects），输出 0-100 分 + 九档定性 + 分维度得分 + 带 文件:行号 证据的问题清单，重点识别 vibe coding / AI 生成的屎山代码。命中关键词：评估项目可维护性 / 给项目打分 / 代码质量评估 / 可读性评分 / AI屎山检测 / vibe coding 体检 / 是否适合人类维护 / HMAP / human maintainability。反触发：用户要求修复、重构、优化、改写代码时不要触发本 skill，本 skill 只评估不修改。"
---

# human-maintainability（HMAP）

Human Maintainability Assessment of Programming Projects —— 评估一个编程项目**是否适合人类程序员维护**，输出 0-100 分与九档定性，并对 vibe coding / AI 生成的屎山代码做专项取证。

**核心原则：本 skill 只评估、不修改。** 评估完成后绝不顺手改代码、不重构、不修 bug；用户没要求修复时，问题只写进报告，不碰源文件。

## 触发与反触发

- **触发**：用户对任意路径的项目提出"评估可维护性 / 给项目打分 / 检查是不是 AI 屎山 / 代码质量如何 / 适不适合人类维护"等意图，且附带或可推断出目标路径。
- **反触发**：用户是要修 bug、重构、加功能、优化性能、写代码——这是别的任务，不要套用本 skill 的评估流程。若用户先评估再提出"顺便修复"，评估完成后按新指令另行处理，不默认包含。

## 评估模型（4 维加权，满分 100）

> **核心原则（打分前必读）**：这是**人类可读 & 可维护性评估**，不是**代码规范审查**。AI 能写出极其规范、命名一致、注释齐全、DRY 的代码，但设计上让人类极其难受。**规范本身不得加分**——只有当规范真正服务于人类理解与维护时才加分。看到表面规范的代码，第一反应不是加分，而是追问：背后的技术思维是什么？承诺成立吗？

> **核心判据（锚定总分）**：读完代码后自问——「这个项目交给你，你愿意亲自维护它吗？为什么？」最终分数必须与这个答案一致。

| 维度 | 权重 | 核心问题 |
|------|------|---------|
| 设计合理性与技术诚实性 | 25 | 架构匹配问题复杂度吗？过度工程还是恰到好处？性能/功能/架构承诺经得起代码验证吗？有技术思维（测量/取舍）还是纯许愿？测试给维护者安全感吗？ |
| 可追踪性与心智负担 | 25 | 人类能追踪"请求→代码"吗？改动一处需知几处（耦合）？逻辑边界清晰还是混成一团（god-file）？需要的脑内存有多大？ |
| 人类可读性与自我解释性 | 20 | 代码能自我解释吗？命名/结构/控制流是帮人懂还是徒有其表？注释是讲"为什么"的给人看，还是复述操作/清单式/写给 AI 看的？README 讲人话且真实吗？ |
| AI 屎山与许愿痕迹（反向计分） | 30 | 承诺-实现背离、为简单问题套模式、无效抽象、为 AI 写的注释、逻辑混杂/god-file、复制粘贴变体、幻觉注释、死代码、吞异常——痕迹越多分越低 |

每维度按 0-100 打分，乘以权重后求和得总分。各维度打分的详细细则见 `references/scoring-rubric.md`；AI 屎山维度的信号清单、许愿检测协议与正反例见 `references/ai-slop-signals.md`。

> **结构性硬闸门**：除维度加权外，若存在使人类无法/极难维护的结构性事实（核心逻辑挤在 >500 行函数或 >2000 行职责混杂文件、核心源码 >50 万行且 god 化、为 AI 编码过程的注释/标记成规模、AI 排查残留混入热路径），总分按 `references/scoring-rubric.md` 的闸门表硬性封顶——维度加权分算得再高也不得超过封顶值。这是"人类是否愿意维护"的客观化判定。

## 九档评分标准（总分映射）

| 分数段 | 档位定性 |
|--------|---------|
| 90-100 | 可读性和人类可维护性极高，注释清晰明确没有废话，架构设计便于后续维护和拓展；即便是世界上最代码洁癖的专业程序员看了也会觉得舒适 |
| 80-90 | 可读性和人类可维护性较高，虽然可能在代码逻辑、架构设计和注释上有一些问题，但整体上较为通畅；大部分高质量开源项目可划入这一档 |
| 70-80 | 正常项目的水平，可维护性、人类可读性不如前两档，日常维护有一定心智负担，但并不多；编码、注释基本规范，基本符合设计实践 |
| 60-70 | 有小部分明显的 AI 屎山痕迹，注释略微混乱，逻辑较为复杂 |
| 50-60 | 有一定规模的 AI 屎山痕迹，部分架构设计意图不明，注释只描述操作而不讲述原因，架构不够规范，有一定的拼凑痕迹，代码较为混乱 |
| 40-50 | AI 屎山痕迹较多，注释垃圾，可维护性较低，人类可读性较差，略微不适宜人类程序员维护 |
| 30-40 | 较为不适宜人类程序员维护 |
| 20-30 | 极为不适合人类程序员维护 |
| 0-20 | 完全就是一坨 AI 拉的屎山，无药可救，不建议人类程序员亲自阅读代码，否则会遭受心智创伤；这种项目有且只能通过 AI agent 维护 |

分数段边界语义：`>=90` 落 90 档；`>=80` 落 80 档；依此类推，`<20` 落 0 档。

档位判定基于**人类维护体验**：一个表面规范、注释齐全但设计让人难受、逻辑混杂的项目，即使"看起来规范"也应落到 50 以下档位；而真正由人类精心维护的优质项目应落在 90 档。

## 评估工作流

**开始前 MUST**：先用 Read 工具读取 [`references/scoring-rubric.md`](references/scoring-rubric.md) 与 [`references/ai-slop-signals.md`](references/ai-slop-signals.md)，统一打分口径与取证信号，再动手。

### Step 1：解析参数

- **目标路径**（必需）：用户指定的项目目录，相对/绝对路径皆可；未指定时默认为当前工作目录。
- `--depth=轻|中|深`（可选）：采样强度，控制读取文件数量。默认"中"。
- `--output=路径`（可选）：把完整报告写入指定 md 文件（路径的父目录需存在）。
- `--focus=维度名`（可选）：只评估指定维度（可读性/注释文档/架构/AI屎山/工程规范），此时不出总分，只出该维度得分与证据。

### Step 2：清点项目

用 Glob / Grep / Bash 统计并记录：

- 文件总数与文件树概况（排除依赖与构建产物目录：`node_modules`、`.git`、`dist`、`build`、`venv`、`.venv`、`target`、`__pycache__`、`.next`、`vendor` 等）
- 语言分布、估计总行数（可只统计源码文件）
- README、构建/包管理入口（`package.json`、`pyproject.toml`、`Cargo.toml`、`go.mod` 等）、入口文件、测试目录的定位

### Step 3：采样协议

在 Step 2 清点结果基础上取样，并在报告中**声明采样范围与置信度**。未读文件不能当读过来打分。

| 项目规模 | 采样策略 | 默认读取量 |
|---------|---------|-----------|
| ≤50 个源码文件 | 全读 | 全部 |
| 50-500 | 入口 + 核心模块 + 各层代表 + 最大文件 + 注释最密集文件 | 15-30 个文件 |
| >500 | 分层采样，覆盖入口、业务核心、边界/基础设施各若干 | 30-60 个文件 |

必读清单（无论规模）：README、构建/包管理入口、入口文件、体积最大的源码文件、最近修改的文件。`--depth=深` 时读取量翻倍；`--depth=轻` 时减半。

### Step 4：逐维度取证

- 按四个维度逐项核对，**每一条评分判断必须落到 `文件:行号` 证据**；只有印象、没有证据的判断不得作为评分依据。
- **承诺-实现核对（必做）**：先读 README / 文档 / 模块说明，列出项目声称的能力、性能、架构承诺；再逐条去代码里验证承诺是否成立（协议见 `references/ai-slop-signals.md` 的许愿检测）。例如声称"性能优化"却只加了抽象层、声称"可扩展"却没有真实扩展点的，记为重大 AI 屎山证据。
- 用 Grep 辅助全局核查：未使用导入/死代码、复制粘贴变体、超大文件与超长函数（god-file）、注释是否复述操作、测试是否只有占位断言等。

### Step 5：加权打分与结构性硬闸门

- 每个维度先给 0-100 的原始分（对照 `references/scoring-rubric.md` 的各档特征），乘以权重后求和。
- **结构性硬闸门（必做）**：按 `references/scoring-rubric.md` 的「结构性硬闸门」表逐条检查（G1 核心 god-function/god-file、G2 体量超人类可维护、G3 为 AI 过程的注释/标记成规模、G4 AI 排查残留混入热路径）。命中即总分硬性封顶，多命中取最低封顶。每条命中必须带 `文件:行号` 证据。
- **判据强制**：最后如实回答「你愿意亲自维护它吗？为什么？」——答案"不愿意"对应总分 ≤60、"完全不可能"≤40。若答案如此而总分未到封顶，说明打分被工程表面（测试/CI/文档纪律）迷惑了，必须下调。
- 总分按九档映射落到档位，取整到整数。
- **打分纪律**：不虚高、不手软，全部基于 Step 4 的证据；证据不足就下调置信度声明，不打虚分。

### Step 6：输出报告

按下方模板输出。默认直接输出到对话；若给了 `--output`，先输出精简版到对话，完整版写入文件并告知路径。

## 报告格式模板

```markdown
## HMAP 评估报告：<项目名>（<目标路径>）

**总分：XX/100 — <档位定性>**

评估范围：共 N 个源码文件，实际读取 M 个（采样深度：轻/中/深），置信度：高/中/低。

### 分维度得分

| 维度 | 原始分 | 权重 | 加权分 | 一句话理由 |
|------|-------|------|-------|-----------|
| 设计合理性与技术诚实性 |  | 25 |  |  |
| 可追踪性与心智负担 |  | 25 |  |  |
| 人类可读性与自我解释性 |  | 20 |  |  |
| AI 屎山与许愿痕迹 |  | 30 |  |  |

### 总体评价

（3-5 句话，说明整体印象与主要矛盾）

### 优点（有据）

- `文件:行号` —— 优点说明
- ...

### 主要问题（有据）

- `文件:行号` —— 问题说明（按严重程度从高到低）
- ...

### AI 屎山专项发现

- `文件:行号` —— 屎山信号（标注属于 `references/ai-slop-signals.md` 的哪类信号）
- ...

### 结论与建议

- 是否适合人类程序员维护（对应九档判语）
- 若得分 <60：说明问题最集中的 1-3 处，以及"建议由人类修还是 AI agent 修"
- 可选改进建议（只建议，不实施）
```

## 边界纪律

1. **只评估不修改**：不重构、不修 bug、不改任何源文件，即使问题再明显。
2. **不虚报证据**：拿不出 `文件:行号` 的判断不许写进报告；引用行号前确认该行确实存在。
3. **诚实打分**：分数必须能从证据推导，采样不足要声明，宁低置信不虚高。
4. **不越范围**：`--focus` 指定维度时只评该维度，不顺手评全部。
